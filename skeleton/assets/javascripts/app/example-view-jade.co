$, templates <- define <[jquery templates]>

class ExampleView
  render: !(element)->
    ($ element)
      &append(templates.example {name:'Jade', css:'CSSHERE'})
      &append(templates["another-example"] {name:'Jade'})

ExampleView
