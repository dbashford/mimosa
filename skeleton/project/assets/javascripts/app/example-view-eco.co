$, templates <- define <[jquery templates]>

class ExampleView
  render: !(element)->
    ($ element)
      &append(templates.example {name:'ECO', css:'CSSHERE'})
      &append(templates.'another-example' {name:'ECO'})

ExampleView