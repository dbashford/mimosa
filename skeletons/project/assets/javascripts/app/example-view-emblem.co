$, templates <- define <[jquery templates]>

class ExampleView
  render: !(element)->
    ($ element)
      &append(templates.example {name:'Emblem', css:'CSSHERE'})
      &append(templates.'another-example' {name:'Emblem'})

ExampleView
